#include <stdlib.h>
#include <string.h>
#include <impl.h>

int array_len(char **a) {
    int count = 0;
    
    return count;
}

int array_cmp(char **a, char **b) {
    int result = TRUE;

    return result;
}

int array_copy(char **dst, char **src, int max_size, int check_after) {
    /*
    ** copy the src array to the dst array up to maxisze ptrs
    ** if check_after is TRUE, the verify that the arrays are the same afterward
    ** if everything checks, return TRUE
    */
    
    int result = TRUE;

    return result;
}

void array_element_swap(char **a, char **b) {

}

int my_cmp(const void *a, const void *b) {
    return 0;
}

void array_sort(char **a, int nel) {
}

void array_sort2(char **a, int nel) {
}
